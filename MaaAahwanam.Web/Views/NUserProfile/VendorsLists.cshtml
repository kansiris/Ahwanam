   <!-- Modal Header -->
<div class="modal-header">
    <h4 class="modal-title">Modal Heading</h4>
    <button type="button" class="close" data-dismiss="modal">&times;</button>
</div>

<!-- Modal body -->
<div class="modal-body">
    Modal Body
    @*<div id="DataTables_Table_0_wrapper" class="dataTables_wrapper form-inline dt-bootstrap">
        <div class="col-sm-12">
            <table class="table table-hover dataTable table-striped width-full dtr-inline" data-plugin="dataTable" id="DataTables_Table_0" border="1">
                <thead>
                    <tr>
                        <th style="font-weight:600">S.No</th>
                        <th style="font-weight:600">Event Type</th>
                        <th style="font-weight:600">User Name</th>
                        <th style="font-weight:600">Booked Dates</th>
                        <th style="font-weight:600">Time Slot</th>
                        <th style="font-weight:600">Deal</th>
                        <th style="font-weight:600">Mobile No</th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        var i = 0;
                        foreach (var item in ViewBag.userorderdates)
                        {
                            <tr>
                                <td>@(++i)</td>
                                <td>@item[0]</td>
                                <td>@item[1] @item[2]</td>
                                <td>@item[3].Replace("00:00:00", "")</td>
                                <td>@item[4]</td>
                                <td>
                                    @if (item[5] == "False")
                                    { <p>No</p>}
                                    else
                                    { <p>Yes</p>}
                                </td>
                                <td>@item[6]</td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>*@
</div>

<!-- Modal footer -->
<div class="modal-footer">
    <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
</div>
