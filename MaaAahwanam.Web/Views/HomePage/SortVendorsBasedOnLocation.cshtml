
<div id="Venues">
    <h4>Venues</h4>
    @foreach (var item in ViewBag.Venue)
    {
        <div class="col-xs-12 col-md-6">
            <div class="checkbox">
                <label>
                    <input type="checkbox" value>
                    <span></span>@item.businessname
                </label>
            </div>
            @if (item.logo != null && item.logo != "")
            {<img src="~/vendorimages/@item.logo" alt="@item.logo">}
            else if (item.image != null && item.image != "")
            {<img src="~/vendorimages/@item.image" alt="@item.image">}
            else
            { <img src="~/noimages.png" alt="No Image Available">}
        </div>
    }
</div>
<div id="Hotels">
    <h4>Hotels</h4>
    @foreach (var item in ViewBag.Hotels)
    {
        <div class="col-xs-12 col-md-6">
            <div class="checkbox">
                <label>
                    <input type="checkbox" value>
                    <span></span>@item.businessname
                </label>
            </div>
            @if (item.logo != null && item.logo != "")
            {<img src="~/vendorimages/@item.logo" alt="@item.logo">}
            else if (item.image != null && item.image != "")
            {<img src="~/vendorimages/@item.image" alt="@item.image">}
            else
            { <img src="~/noimages.png" alt="No Image Available">}
        </div>
    }
</div>
<div id="Resorts">
    <h4>Resorts</h4>
    @foreach (var item in ViewBag.Resorts)
    {
        <div class="col-xs-12 col-md-6">
            <div class="checkbox">
                <label>
                    <input type="checkbox" value>
                    <span></span>@item.businessname
                </label>
            </div>
            @if (item.logo != null && item.logo != "")
            {<img src="~/vendorimages/@item.logo" alt="@item.logo">}
            else if (item.image != null && item.image != "")
            {<img src="~/vendorimages/@item.image" alt="@item.image">}
            else
            { <img src="~/noimages.png" alt="No Image Available">}
        </div>
    }
</div>
<div id="Conventions">
    <h4>Conventions</h4>
    @foreach (var item in ViewBag.Conventions)
    {
        <div class="col-xs-12 col-md-6">
            <div class="checkbox">
                <label>
                    <input type="checkbox" value>
                    <span></span>@item.businessname
                </label>
            </div>
            @if (item.logo != null && item.logo != "")
            {<img src="~/vendorimages/@item.logo" alt="@item.logo">}
            else if (item.image != null && item.image != "")
            {<img src="~/vendorimages/@item.image" alt="@item.image">}
            else
            { <img src="~/noimages.png" alt="No Image Available">}
        </div>
    }
</div>
<div id="Caterers">
    <h4>Caterers</h4>
    @foreach (var item in ViewBag.Catering)
    {
        <div class="col-xs-12 col-md-6">
            <div class="checkbox">
                <label>
                    <input type="checkbox" value>
                    <span></span>@item.businessname
                </label>
            </div>
            @if (item.logo != null && item.logo != "")
            {<img src="~/vendorimages/@item.logo" alt="@item.logo">}
            else if (item.image != null && item.image != "")
            {<img src="~/vendorimages/@item.image" alt="@item.image">}
            else
            { <img src="~/noimages.png" alt="No Image Available">}
        </div>
    }
</div>
<div id="Decorators">
    <h4>Decorators</h4>
    @foreach (var item in ViewBag.Decorator)
    {
        <div class="col-xs-12 col-md-6">
            <div class="checkbox">
                <label>
                    <input type="checkbox" value>
                    <span></span>@item.businessname
                </label>
            </div>
            @if (item.logo != null && item.logo != "")
            {<img src="~/vendorimages/@item.logo" alt="@item.logo">}
            else if (item.image != null && item.image != "")
            {<img src="~/vendorimages/@item.image" alt="@item.image">}
            else
            { <img src="~/noimages.png" alt="No Image Available">}
        </div>
    }
</div>
<div id="Photographers">
    <h4>photography</h4>
    @foreach (var item in ViewBag.Photography)
    {
        <div class="col-xs-12 col-md-6">
            <div class="checkbox">
                <label>
                    <input type="checkbox" value>
                    <span></span>@item.businessname
                </label>
            </div>
            @if (item.logo != null && item.logo != "")
            {<img src="~/vendorimages/@item.logo" alt="@item.logo">}
            else if (item.image != null && item.image != "")
            {<img src="~/vendorimages/@item.image" alt="@item.image">}
            else
            { <img src="~/noimages.png" alt="No Image Available">}
        </div>
    }
</div>
<div id="Mehendi Artists">
    <h4>Mehendi Artists</h4>
    @foreach (var item in ViewBag.Mehendi)
    {
        <div class="col-xs-12 col-md-6">
            <div class="checkbox">
                <label>
                    <input type="checkbox" value>
                    <span></span>@item.businessname
                </label>
            </div>
            @if (item.logo != null && item.logo != "")
            {<img src="~/vendorimages/@item.logo" alt="@item.logo">}
            else if (item.image != null && item.image != "")
            {<img src="~/vendorimages/@item.image" alt="@item.image">}
            else
            { <img src="~/noimages.png" alt="No Image Available">}
        </div>
    }
</div>
